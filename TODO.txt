Main things left to implement for MVP:
- API endpoints and frontend wiring for seasons page episodes (done!)
- API endpoints and frontend wiring for episode downloads (done!)
- API endpoints and frontend wiring for torrent downloads (done!)
- SSE endpoint for download events (done!)
- Download all button/endpoint on season page, maybe pause all/resume all and delete all (done!)
- settings page and API endpoint for settings (done!)
- If episode files are already downloaded in the right location, create a download record for them (done!)
- Scan existing episodes for torrents in flight on qbt, and create records for those (done!)
- Add a confirm delete dialog on the frontend for delete operations (done!)
- Copying metadata only for stuff user has downloaded/wants, not everything (done!)
- Incorporate releases page and extra overrides for proper episode downloads due to breaking changes in source data (done!!)
- initial setup wizard
- Dockerize the full app

Future features:
- If data fetching fails, don't crash app but let it proceed, preventing other data fetches, and giving user notification of error (little yellow banner at top of page)
- If user changes torrent status from qbt instead of the app, make sure the app is updated to reflect that
- Changes in file names and updated episodes (ex redone eps by One Pace team, their crc32 might change, need to think about how to approach this)
- Redo data-fetching for sheets as excel is no longer needed since links are removed, can be much faster with CSV format
- Maybe authentication?
- Proper testing